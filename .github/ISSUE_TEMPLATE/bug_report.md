---
name: Bug report
about: Report a crash, misrender, or benchmark regression
title: "[bug] "
labels: bug
---

## What happened

<!-- What did you see vs. what did you expect? Attach a screenshot if rendering is wrong. -->

## Repro

```bash
# Minimal commands + the smallest Markdown input that triggers it
```

## Environment

- macOS version / chip (e.g. 15.x, M3):
- `zig version`:
- Commit / branch:

## Benchmark impact (if any)

<!-- Paste the failing `[STRICT BENCHMARK]` line. Targets in
`src/core/strict_benchmarks.zig` are immutable — never propose loosening them. -->
