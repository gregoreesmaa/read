# Bare Links

Pasted URLs linkify the way browsers detect them:
https://example.com works mid-sentence, and trailing punctuation
stays literal — see https://example.com/foo.

Parenthesized links keep working (https://example.com/a), while code
spans like `https://example.com` and
[formed links](https://example.com) behave exactly as before.
